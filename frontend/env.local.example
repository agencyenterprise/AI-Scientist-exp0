# Frontend Environment Variables

# API Configuration
# Public base URL that the browser will call (typically the frontend origin)
# For development (Next dev server runs on http://localhost:3000):
NEXT_PUBLIC_API_BASE_URL="http://localhost:3000"
# For production (update with your deployed frontend URL):
# NEXT_PUBLIC_API_BASE_URL="https://your-frontend-app.railway.app"

# Backend API base URL used by Next.js rewrites to proxy /api requests
# For development:
BACKEND_API_BASE_URL="http://localhost:8000"
# For production (update with your Railway backend URL):
# BACKEND_API_BASE_URL="https://your-backend-app.railway.app"

# Environment Settings
NEXT_PUBLIC_ENVIRONMENT="development"
# For production:
# NEXT_PUBLIC_ENVIRONMENT="production"

# Stripe publishable key (used for redirects)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_your_key_here"

# Minimum credits required for major actions (mirrors backend config)
NEXT_PUBLIC_MIN_USER_CREDITS_FOR_CONVERSATION="10"
NEXT_PUBLIC_MIN_USER_CREDITS_FOR_RESEARCH_PIPELINE="25"
